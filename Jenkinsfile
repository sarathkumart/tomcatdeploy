pipeline {
    agent any

    stages {
        stage('Run Shell Commands') {
            steps {
                // Run pwd command
                sh 'pwd'

                // Run ls command
                sh 'ls'
            }
        }
    }
}
